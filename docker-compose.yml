services:
  client-panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tronnus-client-panel
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
